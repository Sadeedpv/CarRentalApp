import React from "react";
import { Navbar } from "../../components/Navbar";

export const HomePage = () => {
    return (
        <div className="flex flex-col w-full overflow-x-hidden items-center h-full">
            <Navbar />
        </div>
    );
}